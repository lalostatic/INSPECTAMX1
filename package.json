{
  "name": "inspectamx-static",
  "private": true,
  "description": "Sitio estático de GitHub Pages. No es la app INSPECTAMX.",
  "scripts": {
    "build": "mkdir -p dist && cp -R index.html 404.html favicon.svg assets dist/ && echo '[gh-pages] estático copiado a dist/'"
  }
}
